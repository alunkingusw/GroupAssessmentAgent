services:
  app:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./config/config.yaml:/app/config/config.yaml:ro
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
